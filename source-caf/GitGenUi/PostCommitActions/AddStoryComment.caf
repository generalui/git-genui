import &StandardImport

(state) ->
  if state extract story, storyId
    log AddStoryComment: {} story, storyId
    &Tracker.tracker.createCommentWithMessage
      story.id
      getCommitComment state
  else
    log skip: AddStoryComment: {} story, storyId

  state