import &StandardImport

getCommitComment: (state) ->
  state extract generatedCommitMessage, storyState, branch, commit, remote

  """
    story #{storyState || :progressed} with commit: `#{commit}`, branch: `#{branch}`, remote: `#{remote?.refs.fetch ? :none}`, message:
    > #{generatedCommitMessage}
    (auto-comment by git-genui v#{&package.version})