import &StandardImport

&Lib.updateStateWithPrompt :story ({stories, story}) ->
  log "" Tip: Select #{&colors.green "(none)"} for no story. It's at the bottom of the story list.
  &GitGenUiPromptFor.story
    stories
    "" What story does this commit advance most?
    story
