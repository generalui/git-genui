import &StandardImport, &Widgets

description: "" add (stage) or remove (unstage) files to commit
run: ->
  log
    """
      Tip: You can also stage files with #{&colors.green "git add"}.

  InteractiveAdd().then -> null
