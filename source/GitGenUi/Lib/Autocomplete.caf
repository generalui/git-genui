import &StandardImport, &SearchSort, {} &sliceAnsi
{}
  autocompleteFromStrings = (strings) ->
    (answersSoFar, input) ->
      process.stdout extract columns

      Promise.resolve
        array str in approximateSearchSort input, strings
          sliceAnsi str, 0, columns - 3
