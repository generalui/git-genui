import &StandardImport, &SearchSort
{}
  autocompleteFromStrings = (strings) ->
    (answersSoFar, input) ->
      process.stdout extract columns

      autocompleteFromStrings input, strings

      Promise.resolve
        array str in searchSort input, strings
          str.slice 0, columns - 3
