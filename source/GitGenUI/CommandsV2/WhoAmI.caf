import &StandardImport

description: "" show which tracker account you are logged in with
run: (options) ->
  &Tracker.myAccount
  .then (account) ->
    log object v from account when !(v is Array) && !(v is Object)
