import &StandardImport

formatStory: (story, addLinks) ->
  idString = "[##{story.id}]"
  idString = &terminalLink idString, &Tracker.getStoryUrl story.id if addLinks

  "" #{idString} (#{story.state}) #{story.name}